# Core FastAPI
fastapi
uvicorn
pydantic
python-dotenv

# Database
sqlalchemy
psycopg2-binary # or asyncpg
alembic
sqlmodel # Optional: If using SQLModel

# Storage
boto3
# minio # Optional: if specific MinIO client features are needed

# Authentication
python-jose[cryptography]
passlib[bcrypt]
fastapi-users[sqlalchemy] # or fastapi-users[sqlmodel]

# AI & File Processing
Pillow
python-magic # Optional
scikit-learn
numpy
spacy
# en_core_web_sm # Downloaded via 'python -m spacy download'
pypdf2
python-docx
openpyxl

# Advanced AI (only if needed)
transformers
torch # or tensorflow

# Advanced CV (only if needed)
opencv-python

# Asynchronous Tasks (only if offloading heavy AI)
celery
redis

# Development & Testing (install in dev environment, not necessarily in prod Docker image)
pytest
httpx
ruff
isort